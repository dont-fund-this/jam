# 
```
 ----------------|----------------------------------------------------------------------------------
| `main.go`             | main entry point          |\                                              |
| `with.go`             | top level wiring          |- these are the jam                            |
| `refs.go`             | maybe a nursery (it isnt) |/                                              |
| `test.go`             | when you want to go test (you know who you are)(so does everyone else)    |
| `play.go`             | when you want to go play                                                  |
| `read.md`             | when you want to read md .. this one. again. .. (wierdo)                  |
| `refs/`               | top level assets - zips, pics, misc,.. (the embedded fs root is here too) |
| `refs/code/`          | egg & chicken `jam efs pull {that} --hard` overwrite `that` (or not) into |<->-> maybe someplace nice. with blackjack and 
| `refs/data/`          | resource files for quivvers (and quivvers) of storage                     | |
| `refs/docs/`          | you like docs. its not your fault.                                        | |
| `refs/html/`          | some.html, some.css, & some.js (that isnt their real name)                | |
| `internal/`           | packages [core|tech|user] <- and sometimes more under those (not always)  | |
| `!**/* ^`             | mostly mistakes, but of course there are obvious exceptions (boctaoe)     | |
| `Makefile`            | except this one, this one is on purpose too                               | |
| `make dev`            | hot reload build rebuild (alledgedly)                                     | |
| `make egg`            | zips this all into `refs/code/code.zip` where it is available at runtime  |>
| `make test`           | the only way to go fast (irony not intended)                              |
| `.considerate.thirds` | the quiet part outloud ina place for things that are not cleaned          |
 -----------------------|--------------------------------------------------------------------------- 

less screaming more comic sans,
behind the wooshop with papyrus.  
this could be a low throaty coo
still some screaming, just less.
tags: [mcsweeneys.net/articles/im-comic-sans-asshole]
todo: move things like this into .considerate.thirds/

```